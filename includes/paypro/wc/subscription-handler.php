<?php

defined('ABSPATH') || exit;

/**
 * Class to handle the payment on returning the WC store.
 */
class PayPro_WC_SubscriptionHandler {
    /**
     * Initializes the actions for the subscription plugins features.
     */
    public function init() {
        if (PayPro_WC_Helper::subscriptionsEnabled()) {


        }
    }
}
